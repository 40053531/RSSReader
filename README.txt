To run the RSSReader you need to:
1. install py_bcrypt via using "py_bcrypt.egg" in src/
2. install feedparser via using "setup.py" in src/feedparser/
3. install SQLite3 via the TCE command "tce-load -wi sqlite3-bin.tcz"
4. run "main.py" with python "python main.py"
